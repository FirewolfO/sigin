# Sign-in 服务开发约束

## 仓库定位

- 本仓库的实际目录和远端名称为 `sigin`，系统职责是云服务的 Sign-in 身份认证后端；除非明确安排仓库迁移，不要自行重命名目录、远端或破坏 Git 历史。
- 使用 Java 和 Spring Boot 实现，负责云服务用户注册、登录、退出、身份校验、密码重置、账号状态和认证凭据生命周期。
- 本服务只确认“用户是谁”。资源、角色和操作权限由独立的 `permission` 系统负责，不要在本仓库复制权限模型或把具体云产品权限硬编码进认证逻辑。
- 本仓库不承载前端页面；登录、注册及云控制台页面放在独立的 `cloud-ui` 仓库，前后端通过明确的 HTTP API 契约协作。

## 技术与工程结构

- 使用 Spring Boot、Spring Web、Spring Security 和 Bean Validation；数据库访问优先使用 Spring Data JPA，数据库结构变更使用 Flyway 管理。
- 构建工具统一使用 Maven，并提交 Maven Wrapper。Java、Spring Boot 和依赖版本必须在构建文件中固定；升级主版本前先确认兼容性并补充迁移说明。
- Java 包按职责拆分为 API/Controller、Application Service、Domain、Repository 和 Infrastructure。Controller 只负责协议转换、输入校验和响应封装，不直接编写持久化或复杂业务逻辑。
- 配置使用 `application.yml`、环境变量或外部配置注入；环境地址、数据库密码、Cookie 密钥、Token 密钥和第三方凭据不得写入代码或提交到 Git。
- 对外 API 使用版本前缀，例如 `/api/v1`。契约变化应同步 OpenAPI、`document` 中对应系统文档以及受影响的 SDK，避免前后端依赖未声明的响应字段。

## 认证与安全

- 认证能力必须基于 Spring Security 等成熟实现，不得自行设计密码摘要、Token 签名或加密算法。
- 密码只能保存经过专用 `PasswordEncoder` 处理的不可逆摘要，优先选择经过评审的 Argon2id 或 BCrypt 参数；禁止保存、记录、回显或通过消息传递明文密码。
- 登录失败响应应避免泄露账号是否存在；注册、登录、验证码、密码重置和 Token 刷新接口必须具备频率限制、重放防护和必要的审计记录。
- 访问凭据应短时有效，刷新凭据必须支持轮换和吊销。面向浏览器时优先使用 `HttpOnly`、`Secure`、合理 `SameSite` 的 Cookie，并同时设计 CSRF 防护；不得要求前端将长期 Token 保存到 `localStorage`。
- 用户名、邮箱或手机号的规范化和唯一性必须由服务层校验并由数据库唯一约束兜底。验证码、重置令牌等一次性凭据必须设置过期时间且使用后立即失效。
- 日志和审计信息不得包含密码、验证码、完整 Token、Cookie、密钥或其他可直接用于登录的敏感数据。面向用户的错误与内部异常必须分离。

## 数据与接口

- 用户、认证凭据、刷新会话和必要审计数据必须持久化到数据库；所有表、索引和约束必须通过 Flyway migration 留存在仓库中，不得只依赖 ORM 自动建表。
- 注册、登录和账号状态变更需要清晰的事务边界。跨服务调用必须设置超时、有限重试和幂等策略，不得在数据库事务中进行无界外部调用。
- API 响应应包含稳定的错误码和请求 ID。输入字段必须设置长度、格式和取值范围限制，禁止直接将持久化实体作为 API 请求或响应模型。
- 时间统一以 UTC 持久化和传输；展示时区由调用方处理。

## 质量与工作流

- 业务规则必须有单元测试；注册、登录、退出、凭据刷新、账号禁用和安全失败路径必须有集成测试。涉及数据库约束时使用真实兼容数据库或 Testcontainers 验证。
- 提交前至少运行 Maven 测试、静态检查和项目已有的格式化检查；不得通过关闭安全校验或删除失败测试来完成构建。
- 本仓库是独立 Git 仓库。每次修改完成并通过必要验证后，必须直接创建提交并推送当前分支；提交中不得混入其他仓库或无关文件。
